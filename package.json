{
  "name": "nocatee-paver-sealing-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp *.html public/ && cp styles.css public/ && cp *.js public/ && cp robots.txt public/ && cp sitemap.xml public/ && cp *.webp public/ && cp *.png public/ && (cp *.jpg public/ 2>/dev/null || true) && (cp *.jpeg public/ 2>/dev/null || true)",
    "start": "vercel dev"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
